#!groovy
// Check ub1 properties
properties([disableConcurrentBuilds()])

pipeline {
    agent { 
        label 'jenkins'
        }
    options {
        buildDiscarder(logRotator(numToKeepStr: '10', artifactNumToKeepStr: '10'))
        timestamps()
    }
    stages {
        stage("First step") {
            steps {
                sh 'ssh vova@linux2 \'hostname\''
            }
        }
        stage("Second step") {
            steps {
                sh 'ssh vova@linux2 \'uptime\''
            }
        }
    }
}
